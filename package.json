{
	"name": "vscode-vba",
	"displayName": "VBA",
	"description": "Visual Basic for Applications support for VS Code.",
	"icon": "images/icon_256x256.png",
	"version": "0.2.1-dev",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/serkonda7/vscode-vba"
	},
	"publisher": "serkonda7",
	"engines": {
		"vscode": "^1.56.0"
	},
	"categories": [
		"Formatters",
		"Programming Languages"
	],
	"activationEvents": [
		"onLanguage:vba"
	],
	"main": "./out/extension.js",
	"contributes": {
		"languages": [
			{
				"id": "vba",
				"aliases": [
					"VBA"
				],
				"extensions": [
					".bas",
					".cls",
					".frm"
				],
				"configuration": "./vba.configuration.json"
			}
		],
		"grammars": [
			{
				"language": "vba",
				"scopeName": "source.vba",
				"path": "./syntaxes/vba.tmLanguage.json"
			}
		]
	},
	"scripts": {
		"compile": "tsc -p ./",
		"watch": "tsc -watch -p ./",
		"lint": "eslint .",
		"test": "vscode-tmgrammar-test -s source.vba -g syntaxes/vba.tmLanguage.json -t \"syntaxes/tests/*.bas\"",
		"package": "vsce package",
		"install-dev-ext": "npm install && npm run test && vsce package -o ./out/vscode-vba-dev.vsix && code --install-extension ./out/vscode-vba-dev.vsix --force",
		"vscode:prepublish": "npm run compile",
		"postinstall-dev-ext": "echo 'Please reload the window (run `>reload window` from command palette)'"
	},
	"devDependencies": {
		"@types/node": "^14.18.12",
		"@types/vscode": "~1.56.0",
		"@typescript-eslint/eslint-plugin": "^5.17.0",
		"@typescript-eslint/parser": "^5.17.0",
		"eslint": "^8.12.0",
		"typescript": "~4.6.3",
		"vsce": "^2.7.0",
		"vscode-tmgrammar-test": "~0.0.11"
	}
}
